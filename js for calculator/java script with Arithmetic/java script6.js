let firstNumber
let secondNumber
let step = 0
let operation
let result = 0

const numArray = []
const secondNumArray = []

const display = document.getElementById('display')
function getNumber(num) {
    if (step === 0
        
    ) {
        numArray.push(num)